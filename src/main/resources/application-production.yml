spring:
  datasource:
    url: "jdbc:postgresql://postgresql-release.default.svc.cluster.local:5432/campsite?currentSchema=public"
    username: campsite
    password: campsite
  jpa:
    database-platform: "org.hibernate.dialect.PostgreSQL9Dialect"